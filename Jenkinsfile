pipeline {
    agent any

    stages {
        stage('Connexion GitHub') {
            steps {
                echo 'Jenkins est connecté à GitHub ✅'
            }
        }

        stage('Test Build') {
            steps {
                sh 'echo Build réussi'
            }
        }
    }
}
